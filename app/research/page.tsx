import {Header,Footer} from '../components'; import {researchPosts,site} from '../data';
export const metadata={title:`Research | ${site.brand}`,description:'Original research and source-backed analysis for planning Philippines-based support.'};
export default function Research(){const clusters=['All Research','Hiring Controls','Scope Benchmarks','Workflow Design'];const pageSize=20;const pages=Math.max(1,Math.ceil(researchPosts.length/pageSize));const posts=researchPosts.slice(0,pageSize);return <><Header/><main className="research-index-page"><section className="research-hero"><div className="container research-hero-grid"><div><p className="eyebrow">Research Library</p><h1>Source-backed research for better staffing decisions</h1><p className="lead">Use these reports to compare roles, controls, and onboarding decisions before building a Philippines-based support team.</p><div className="research-meta"><span>{researchPosts.length} reports</span><span>10 sources per report</span><span>Buyer controls</span></div></div><aside className="research-hero-card" aria-label="Research quality signals"><div><strong>01</strong><span>Benchmarks and operating context</span></div><div><strong>02</strong><span>Practical screening questions</span></div><div><strong>03</strong><span>Implementation checks</span></div></aside></div></section><section className="section research-library-section"><div className="container"><nav className="research-cluster-tabs" aria-label="Research topic filters">{clusters.map((cluster,i)=><a className={i===0?'active':''} href="/research" key={cluster}>{cluster}<small>{i===0?researchPosts.length:researchPosts.filter(p=>p.cluster===cluster).length}</small></a>)}</nav><div className="research-card-grid">{posts.map(p=><a className="research-library-card" href={`/research/${p.slug}`} key={p.slug}><span className="research-card-badge">{p.cluster}</span><h2>{p.title}</h2><p className="research-card-highlight">Headline signal: {p.headlineStat}.</p><p className="research-card-excerpt">{p.excerpt}</p><div className="research-card-meta"><span>{site.brand} Research</span><span>8 min read</span><span>{p.sources.length} sources</span></div></a>)}</div><nav className="pagination" aria-label="Research pages">{Array.from({length:pages},(_,i)=><a aria-current={i===0?'page':undefined} href={i===0?'/research':`/research/page/${i+2}`} key={i}>{i+1}</a>)}</nav></div></section><section className="section research-methodology"><div className="container"><h2>Methodology and use</h2><p>Each report separates sourced facts from recommendations, names limitations, and translates findings into a role brief or workflow your team can review.</p></div></section></main><Footer/></>}
