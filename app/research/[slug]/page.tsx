import {notFound} from 'next/navigation';
import {Header,Footer,CTA} from '../../components';
import {researchPosts} from '../../data';
export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);return p?{title:`${p.title} | Outsourced Account Management`,description:p.excerpt}:{};}
export default async function ResearchPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)notFound();return <><Header/><main className="section"><article className="container guide-article"><p className="eyebrow">{p.cluster} · Research report</p><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><p className="research-card-meta">Published {p.published} · Updated {p.updated} · {p.sources.length} sources</p><div className="card"><h2>Headline signal</h2><p><strong>{p.headlineStat}.</strong> Source: {p.statSource}. This is contextual evidence, not a claim about this company or a performance guarantee.</p><h2>Key takeaways</h2><ul>{p.takeaways.map(x=><li key={x}>{x}</li>)}</ul>{p.sections.map(s=><section key={s.heading}><h2>{s.heading}</h2>{s.paragraphs.map(x=><p key={x}>{x}</p>)}</section>)}<h2>Review table</h2><table><caption>Research control checklist</caption><thead><tr>{p.table.headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{p.table.rows.map(row=><tr key={row[0]}>{row.map(cell=><td key={cell}>{cell}</td>)}</tr>)}</tbody></table><h2>Sources</h2><ol>{p.sources.map(s=><li key={s.url}><a href={s.url} rel="noreferrer">{s.name}</a> — {s.date}. {s.note}</li>)}</ol><h2>Questions to review</h2>{p.faqs.map(f=><div key={f.q}><h3>{f.q}</h3><p>{f.a}</p></div>)}<h2>Related research</h2><ul>{p.related.map(slug=>{const r=researchPosts.find(x=>x.slug===slug);return r?<li key={slug}><a href={`/research/${slug}`}>{r.title}</a></li>:null})}</ul><p>Next steps: <a href={p.internalLinks[0].href}>{p.internalLinks[0].label}</a> or <a href={p.internalLinks[1].href}>{p.internalLinks[1].label}</a>.</p></div></article><CTA/></main><Footer/></>}
